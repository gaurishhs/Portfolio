---
export interface KV {
    key: string;
    value: string;
    link: string;
}

interface Props {
    items: KV[];
}

const { items } = Astro.props;
---

<ul class="divide-y w-full divide-zinc-700">
    {items.map((item, i) => (
        <li
            class="py-3 w-full justify-between flex gap-2 cursor-pointer"
        >
            <a
                class="flex w-full group justify-between text-sm text-zinc-500"
                href={item.link}
            >
                <span class="mr-2 transition group-hover:text-zinc-400">
                    {item.key}
                </span>
                <p class="transition group-hover:text-zinc-400">
                    {item.value}
                </p>
            </Link>
        </li>
    ))}
</ul>

